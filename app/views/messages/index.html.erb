<h1><%= @susu.name %> Messages</h1>

<div class="messages-container">
  <% @messages.each do |message| %>
    <p><%= message.content %></p>
  <% end %>
</div>

<%= simple_form_for [@susu, @message] do |f| %>
  <%= f.input :content, label: false %>
  <%= f.submit "Send Message"  %>
<% end %>
